package com.seedlegals.demo

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test
import java.time.Instant

internal class StopWatchTest {
/**
    @Test
    fun testStart() {
        val startTimeInstant: Instant = ???

        val unit = StopWatch()

        val result = unit.start()

        assertEquals(startTimeInstant, result)
    }

    @Test
    fun testEnd() {
        val stopTimeInstant: Instant = ???

        val unit = StopWatch()

        val result = unit.stop()

        assertEquals(stopTimeInstant, result)
    }

    @Test
    fun testDifference() {
        val expectedDifference: Long = ???

        val unit = StopWatch()

        unit.start()
        unit.stop()

        val result = unit.difference()

        assertEquals(expectedDifference, result)
    }
**/
}